package loginexample.Entities.Interfaces;

public interface IFreno {
    // Método para aplicar el freno
    void aplicarFreno();

    // Método para liberar el freno
    void liberarFreno();

    // Método para verificar el estado del freno
    boolean estadoFreno();
}